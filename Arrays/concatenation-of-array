class Solution {
    public int[] getConcatenation(int[] nums) { 

    int n = nums.length;         // 3 (in question)

    int[]ans = new int[2 * n];   //double the array size,so here n = 2*3 = 6
    
    for(int i = 0;i < n; i++){ 

       ans[i] = nums[i];         //iterate i= 0 -->ans[0] = nums[0] --> ans[0] = 1

       ans[i+n] = nums[i];       //ans[0+3] = nums[i]  = ans[3]  = 1
    }

    return ans;                   // output :[1,0,0,1,0,0];


    }
}

// EXPLANATION

/* First, I create a variable `n` to store the length of the `nums` array.

Then, I create an array called `ans` with double the size of the `nums` array.

Next, I use a `for` loop to traverse the `nums` array.

In each iteration, I copy the current element from the `nums` array to the `ans` array.

I store the first copy at `ans[i]` and the second copy at `ans[i + n]`.

Finally, I return the `ans` array.
*/




// time complexity

// The algorithm traverses the array only once. Therefore, the time complexity is O(n)."



// space complexity

// I don't create any extra array or data structure. I modify the original array, so the space complexity is O(1)




// Can it be optimized?

// To calculate the running sum, we must visit every element at least once.
// So we cannot do better than O(n).

